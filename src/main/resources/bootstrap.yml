spring:
  application:
    name: business-logic-rest
  cloud:
    config:
      uri: https://maisvida-configuration-server.cfapps.io/
      password: ${config.server.password}
      username: ${config.server.user}
  profiles:
    active: development
