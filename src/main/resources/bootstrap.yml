spring:
  application:
    name: business-logic-rest-config
  cloud:
    config:
      uri: https://maisvida-configuration.cfapps.io/
      password: ${config.server.password}
      username: ${config.server.user}
  profiles:
    active: development
